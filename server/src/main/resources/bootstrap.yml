spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: test
#stream可以配置到git上去
    stream:
      bindings:
        input:
          destination: mqTestDefault
        output:
          destination: mqTestDefault
          contentType: text/plain
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

